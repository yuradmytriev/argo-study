# =============================================================================
# BASE SERVICE - Shared service definition
# =============================================================================

apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-service
spec:
  type: ClusterIP                           # Base uses ClusterIP (internal only)
  selector:
    app: gitops-demo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
